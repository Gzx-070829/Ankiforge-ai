# DeckStyleProfile (v0.14.0)

The immutable profile model and bounded statistics summarizer are implemented. Real collection sampling is foundation-only: it remains an opt-in disabled preview until manual Anki acceptance verifies the operation. The intended limit is one selected deck and at most 20 notes; statistics, not complete card bodies, are the default eligible context. No full collection scan or note mutation is allowed.
