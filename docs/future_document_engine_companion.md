# Future document engine companion (v0.14.1)

The companion is protocol foundation only, not a shipped application. Any future local process must remain explicit, local-file-only, bounded, and independently licensed/security-reviewed. It must not imply cloud import, model download, automatic installation, or a bundled executable.
