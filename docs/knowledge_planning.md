# Knowledge planning (v0.14.0)

The local planner balances sections, keeps source chunk references, removes repeated points, and selects supported templates. It is deterministic and does not contact a Provider. A later Provider-assisted stage remains within the chosen call budget and must not add out-of-source content. Planning/coverage is a review aid, not a guarantee that every fact is complete or correct.
