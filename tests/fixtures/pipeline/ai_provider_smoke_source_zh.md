# 监督学习

监督学习使用带有输入和目标标签的样本训练模型。模型学习从输入到目标的映射，
并应当在未参与训练的新样本上保持良好的预测能力。

## 过拟合

过拟合是指模型过度贴合训练数据中的细节和噪声。此时训练误差可能很低，
但模型在验证数据或真实新数据上的误差会升高。

## 验证集

验证集不参与模型参数的直接训练，而是用于比较模型、选择超参数和观察泛化表现。
反复依据同一个验证集调整方案，也可能使决策逐渐过度适应该验证集。
